#include <stdbool.h>
#include <stdio.h>

int main(void) {
  if (true) {
    printf("true value is supported in C99 or later\n");
  }
  if (!false) {
    printf("false value is supported in C99 or later\n");
  }
}
